# Copy to .env on the droplet and set real values. Both files are gitignored.
MARIADB_ROOT_PASSWORD=change-me-root
MARIADB_PASSWORD=change-me-xiadmin

# Public address clients connect to. Written into zone_settings.zoneip after every
# dbtool update by the 'zoneip' service; without it clients hang at 'Downloading data...'.
SERVER_IP=203.0.113.10
